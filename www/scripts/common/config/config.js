/**
 * global config
 *
 * @module Pixes.common
 * @author Erichain
 * @date 2015-10-31
 */

(function( Common ) {

    Common.factory('config', [function() {
        return {

        };
    }]);

})( angular.module('Pixes.common') );