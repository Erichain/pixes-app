/**
 * favorites page's controller
 *
 * @author Erichain
 * @date 2015-10-25
 */
(function ( favorites ) {
    var FavoritesCtrl = function ( $scope ) {

    };

    favorites.controller('FavoritesCtrl', FavoritesCtrl);
    FavoritesCtrl.$inject = ['$scope'];
})( Pixes.favorites );