/**
 * @description api for start page and access
 * @module Pixes.start
 * @author Erichain
 * @date 2015-04-07
 */
(function ( Start ) {

    Start.constant('StartApi', {
        request: '/get_request'
    });

})( angular.module('Pixes.start') );